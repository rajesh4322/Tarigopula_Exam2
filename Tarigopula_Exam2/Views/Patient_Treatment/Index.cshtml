@model IEnumerable<Tarigopula_Exam02.Models.Patient_Treatment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Physician.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Treatment.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient_Treatment_Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Physician.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Treatment.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient_Treatment_Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Patient_TreatmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Patient_TreatmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Patient_TreatmentID })
        </td>
    </tr>
}

</table>
